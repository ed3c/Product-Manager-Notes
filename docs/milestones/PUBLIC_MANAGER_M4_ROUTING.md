# Public Manager M4 Evidence Routing

Status: `PASS_BOUNDED_AS_ROUTING`

This milestone advances the public-safe Manager control plane from design/evidence-audit routing to an exact executable reviewer subject in `ed3c/DevOps-Manager-Notes`.

It does **not** convert repository evidence into employment tenure, people-management history, production adoption or production incident history.

## Exact public executable subject

```text
repository      ed3c/DevOps-Manager-Notes
issue           #9
pull request    #44
source commit   55d18cdc556ca5d66c67406318ae25196c077fd2
source tree     a4ad37d9baf73a5239f79c516c67b0182420336a
milestone       PUBLIC_REMOTE_REVIEWER_CONVERGENCE_FIRST_GREEN
status          PASS_BOUNDED
```

### Deterministic reviewer evidence

```text
workflow        Full MVP reviewer convergence
run             32256802856
artifact        9366615717
digest          sha256:e652cdf3ccb8de7a8655f9148bc6471a29bcaeaef2d55f6255bca2cb2b1e19d3
ceiling         GITHUB_HOSTED_REMOTE_REVIEWER_CONVERGENCE_ONLY
```

### Artifact re-verification evidence

```text
workflow        Full MVP public reviewer convergence
run             32256802554
artifact        9366596481
digest          sha256:696f56be2fb637e386941eb313caa7c9448900ad0bad88b3a96119b95315596c
ceiling         GITHUB_HOSTED_ARTIFACT_REDOWNLOAD_AND_REVIEWER_BUNDLE_ONLY
```

The second workflow re-downloads six bound M2/M3 Actions artifacts, verifies their archive SHA-256 identities and admits required public-safe files into one reviewer packet.

## Manager evidence path

```text
job requirement
→ product/system problem
→ invariant / SLO / evidence contract
→ technology and build-vs-buy decision
→ Tech Lead dependency DAG
→ Core implementation
→ observability / policy / ML lifecycle / UI / supply-chain leaves
→ failure/recovery convergence
→ same-failure re-test
→ exact reviewer convergence
→ public evidence packet
→ bounded interview narrative
```

## Role coverage

### Technical Product Manager

M4 supports bounded technical evidence for:

```text
AI/ML platform system design
model/prompt/config lifecycle reasoning
MLflow evaluation/promotion/rollback workflow
internal platform product journey
cross-plane requirements → implementation traceability
technical communication through explicit contracts and evidence ceilings
platform adoption/scaling design discussion
failure-driven prioritization and corrective change reasoning
```

It does **not** prove:

```text
5+ years ML/Data/Platform employment tenure
2+ years formal TPM/Program Management tenure
real 1,000-user internal adoption
real production model traffic
historical stakeholder or direct-report management claims
```

Those remain `HUMAN_ADMIT_REQUIRED` or outside repository proof.

### DevOps Manager

The same public evidence packet contains bounded proof for:

```text
Python platform implementation
CI/CD and immutable artifact contracts
container/Kubernetes desired-state design
observability and SLO mechanics
OPA / Trivy / SBOM / signing gates
incident command and recovery drills
same-failure re-verification
one-command reviewer flow
```

Local/production substrate residuals remain visible.

## Shadow Architect claim matrix

| Claim | State | Ceiling / reason |
|---|---|---|
| deterministic reviewer flow runs without paid model API | `PASS` | GitHub-hosted reviewer convergence only |
| exact M2/M3 artifacts can be re-downloaded and digest-verified | `PASS` | six bound Actions artifacts only |
| seven failure/recovery DRILLs re-verify | `PASS` | DRILL/SIMULATION, not production incident history |
| Demo Console is exact admitted byte input | `PASS` | PR #37 evidence-head parity only |
| live kind/Kubernetes | `NOT_EXERCISED` | Local Handoff required |
| real Argo CD/Rollouts runtime | `NOT_EXERCISED` | Local Handoff required |
| local Qwen/llama.cpp | `NOT_EXERCISED` | exact model/runtime binding required |
| 1,000 VU capacity/recovery | `NOT_EXERCISED` | synthetic workload not yet run |
| 1,000 real users | `OUTSIDE_REPOSITORY_PROOF` | cannot be inferred from virtual load |
| production incident history | `OUTSIDE_REPOSITORY_PROOF` | DRILL cannot promote to production history |
| people-management / TPM tenure | `HUMAN_ADMIT_REQUIRED` | employment evidence only |

## Local Handoff

DevOps PR #45 is the current Local Handoff child.

```text
queue item       M4-LOCAL-REVIEWER-001
command          bash scripts/demo/run_reviewer_demo.sh evidence/local-reviewer
expected receipt evidence/local-reviewer/reviewer-demo-receipt.json
```

The GitHub queue-contract validation is green, but the local command itself remains `NOT_EXERCISED` until a local host returns the receipt. The following live-substrate item remains `BLOCKED_UNRESOLVED` and has no fabricated executable command.

## Google / GitHub boundary

GitHub remains canonical. Existing Google Doc/Sheet URLs in `registry/external-links.yaml` remain human narrative/dashboard edges with `evidence_authority: NONE`.

A Google statement may summarize M4 only after it points back to the exact public subjects above. Google content cannot promote any `NOT_EXERCISED`, `DRILL`, local-runtime, virtual-load or Human-admit state.

## Next Product frontier

```text
M4 exact public evidence
        +
Product existing evidence audit PR #9
        +
public disclosure contract PR #11
        ↓
issue #4 interview/evidence convergence
        ↓
public-safe interview story / portfolio route
```

This routing milestone is complete at its bounded scope. Issue #4 remains open until the human-facing narrative/dashboard/export surfaces consume these exact subjects and preserve all residual evidence boundaries.
